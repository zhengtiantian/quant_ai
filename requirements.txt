# quant_ai dependencies

# Web framework
fastapi>=0.118.0
# Not pinned exactly: mcp requires uvicorn>=0.31.1, so an exact pin here silently
# conflicts with any mcp upgrade. The pin at 0.31.0 broke the image build once the
# MCP dependency landed — the local venv already had a newer uvicorn, so only a
# clean install surfaced it.
uvicorn>=0.31.1
requests==2.31.0
pydantic==2.12.5
python-dotenv==1.0.1

# LLM clients (LangChain used only for ChatOpenAI wrapper + message types)
langchain==0.3.7
langchain-core>=0.3.17,<0.4.0
langchain-openai==0.2.2
langchain-anthropic==0.3.0
openai==1.55.3
anthropic>=0.40.0

# Vector math (RAG cosine similarity)
numpy>=1.26.0

# MCP — mcp_server.py exposes the platform's tools over stdio; mcp_client.py
# is how the agent consumes them. Requires fastapi>=0.118 for starlette.
mcp>=1.28.0

# Mongo fallback inside mcp_server.py, used when quant_api is unreachable
pymongo>=4.6.0

# R.1 vector store — tools/index_news.py and tools/search_news.py. Additive: the
# install pulls grpcio/protobuf/h2/portalocker and downgrades nothing existing.
qdrant-client>=1.12

# Async / HTTP transitive deps
aiohttp>=3.9.0,<4.0.0
PyYAML>=6.0.0
tenacity>=8.1.0,<10.0.0
