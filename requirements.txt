# quant_ai dependencies

# Web framework
fastapi>=0.118.0
uvicorn==0.31.0
requests==2.31.0
pydantic==2.12.5
python-dotenv==1.0.1

# LLM clients (LangChain used only for ChatOpenAI wrapper + message types)
langchain==0.3.7
langchain-core>=0.3.17,<0.4.0
langchain-openai==0.2.2
langchain-anthropic==0.3.0
openai==1.55.3
anthropic>=0.40.0

# Vector math (RAG cosine similarity)
numpy>=1.26.0

# MCP client — agent tools are discovered from quant_mcp over stdio, not
# implemented here (see mcp_client.py). Requires fastapi>=0.118 for starlette.
mcp>=1.28.0

# Async / HTTP transitive deps
aiohttp>=3.9.0,<4.0.0
PyYAML>=6.0.0
tenacity>=8.1.0,<10.0.0
